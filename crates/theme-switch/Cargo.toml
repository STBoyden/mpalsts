[package]
name = "mpalsts_theme_switch"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
log.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
osakit = { version = "0.3", features = ["full"] }
